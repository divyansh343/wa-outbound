create a next js project on docker that can be dployed on vps, that do outbound for collected leads from indiamart seller account, we are making a outbound saas for indiamart sellers so make for multiple orgs multi tenant system and inside we can analyze, leads and do oubound using wa-account using baileys whatsapp, and for each org add leads in lead pool and divide leads between many wa-account users a campaign has multiple wa-account for outreach and do outreach and follow on a systamtic manner and can tweak volume, delay and lead on wa-account and also a default settings example chat like in chat.txt and can also see connect and reply from dash , and also have systamtic activity notification give notification to reply so an advanced outbound os , make schema on neon db accordin to it needed and do your best and can also sync new leads from a google sheet , and we can manage everything from dashbord and also have a feature to upload leads in bulk using csv file, and also have a feature to track the performance of each wa-account and campaign, and also have a feature to schedule the outreach for specific time and date, and also have a feature to automate the follow-up messages based on the response from the leads, and also have a feature to integrate with other tools like CRM, email marketing etc. use tailwind css and JS not TS and also use next auth for authentication and authorization, and also use redis for caching and session management, and also use bull for job queue management, and also use socket.io for real-time communication between the server and the client, and also use winston for logging, and also use jest for testing, and also use eslint for code linting, and also use prettier for code formatting, and also use husky for git hooks, and also use lint-staged for running linters on staged files, and also use commitlint for commit message linting, and also use semantic-release for automated releases, and also use docker-compose for managing multiple containers, and also use nginx as a reverse proxy for the application, and also use certbot for SSL certificate management, and also use pm2 for process management in production. do your best to make it scalable and maintainable, and also make sure to follow best practices for security and performance. & explicitely we use baileys whatsapp for wa-account management and outreach, and also use the following connection string to connect to the postgresql database:  


postgresql://neondb_owner:npg_egox6ICBUc1F@ep-calm-union-ao5z58ec-pooler.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require



not use any orm do direct SQL queries for database operations, and also make sure to handle errors and edge cases properly, and also make sure to write clean and maintainable code, and also make sure to document the code properly, and also make sure to use environment variables for sensitive information like database connection strings, API keys etc., and also make sure to use a .env file for local development, and also make sure to use a .env.production file for production environment, and also make sure to use a .env.example file to provide an example of the required environment variables, and also make sure to use a .gitignore file to ignore the .env files and other sensitive files from being committed to the repository.